function [theta] = normalEqn(X, y)
%
%   
%   

theta = zeros(size(X, 2), 1);

%

% ---------------------- Sample Solution ----------------------

X_inv = inv(X'*X); 
theta = X_inv*X'*y;

% -------------------------------------------------------------


% ============================================================

end
